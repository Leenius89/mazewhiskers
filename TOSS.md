# 메이즈 위스커스 — 앱인토스 빌드

이 폴더(`mazewhiskers-toss`)와 `toss` 브랜치는 **앱인토스 전용**입니다.
웹·전시 버전은 `my-react-game` 폴더의 `main` 브랜치이고, 서로 섞지 않습니다.

- `toss`는 `main`에 **머지하지 않습니다.** `main`은 Vercel로 바로 배포됩니다.
- 게임 자체의 수정(버그, 밸런스)은 `main`에서 하고, 여기서 `git merge main`으로 가져옵니다.

## 무엇이 다른가

| | 웹 (`main`) | 토스 (`toss`) |
|---|---|---|
| 랭킹 | 이름 입력 → Supabase 4종 | 토스 게임센터 1종, 입력 없음 |
| 점수 | 모은 것 | (모은 것 + 도착 1,000 + 시간 보너스) × 난이도 |
| 내 기록 | 없음 | 사용자 식별키별로 기기에 저장 |
| 백그라운드 | 처리 없음 | 소리 정지 + 일시정지 |
| 뒤로가기 | 없음 | 한 단계씩 닫기 → 종료 확인 |
| 화면 | 가로·세로 | 세로 고정, 토스 버튼 자리 비움 |
| 폰트 | CDN | 번들에 포함 |
| 음악 | 스테레오 256k | 모노 128k |

토스와 닿는 코드는 전부 `src/platform/`에 있습니다. SDK는 `src/platform/toss.ts`
한 곳에서만 import합니다. SDK 함수는 토스 밖에서 호출하면 예외를 던지기 때문에,
이 파일이 모든 호출을 감싸서 일반 브라우저에서는 조용히 넘어가게 합니다.
그래서 같은 빌드가 브라우저에서도 그대로 돌아갑니다(등급분류 제출용).

## 빌드

```bash
npm ci
```

로컬에서 눈으로 확인할 때 (토스 버튼·노치를 흉내 낸 화면):

```bash
CI=false GENERATE_SOURCEMAP=false REACT_APP_TOSS_SIM=1 npx react-scripts build
```

빌드를 서버로 띄운 뒤 주소 끝에 `?tossSim=1`을 붙입니다. 콘솔에서 `__tossBack()`을
부르면 안드로이드 뒤로가기를 누른 것과 같습니다. 이 스위치는 출시 빌드에는 없습니다.

**토스에 올릴 번들은 로컬에서 만들지 않습니다.** Windows의 `ait build`는 번들 안
경로를 역슬래시로 적습니다. GitHub Actions(리눅스)가 만듭니다.

- `toss` 브랜치에 push → 번들을 만들어 Actions 결과물로 남김
- `toss-`로 시작하는 태그를 push → 번들을 만들어 **토스 콘솔에 업로드** (QR 테스트 가능)

```bash
git tag toss-0.1.0-1
```

```bash
git push origin toss-0.1.0-1
```

콘솔 업로드에는 GitHub 저장소 Secret `AIT_API_KEY`가 필요합니다
(콘솔 → 워크스페이스 → 왼쪽 메뉴 "키"에서 발급).

업로드는 출시가 아닙니다. 콘솔에서 QR 테스트 → 검토 요청 → 승인 → "출시하기"를
직접 눌러야 사용자에게 나갑니다.

## 바꾸면 안 되는 것

- `apps-in-toss.config.ts`의 `appName: 'mazewhiskers'` — 콘솔 등록값과 같아야 하고,
  콘솔에서는 등록 후 수정·삭제가 안 됩니다.
- 게임 안의 이름 "메이즈 위스커스" — 콘솔 앱 이름, 등급분류 신청서의 게임물명과
  같아야 합니다.
