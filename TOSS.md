# 메이즈 위스커스 — 앱인토스 빌드

이 폴더(`mazewhiskers-toss`)와 `toss` 브랜치는 **앱인토스 전용**입니다.
웹·전시 버전은 `my-react-game` 폴더의 `main` 브랜치이고, 서로 섞지 않습니다.

- `toss`는 `main`에 **머지하지 않습니다.** `main`은 Vercel로 바로 배포됩니다.
- 게임 자체의 수정(버그, 밸런스)은 `main`에서 하고, 여기서 `git merge main`으로 가져옵니다.

## 무엇이 다른가

| | 웹 (`main`) | 토스 (`toss`) |
|---|---|---|
| 랭킹 | 이름 입력 → Supabase 4종 | 토스 게임센터 1종, 입력 없음 |
| 점수 | 모은 것 | (모은 것 + 도착 1,000 + 시간 보너스) × 난이도 |
| 내 기록 | 없음 | 사용자 식별키별로 기기에 저장 |
| 백그라운드 | 처리 없음 | 소리 정지 + 일시정지 |
| 뒤로가기 | 없음 | 한 단계씩 닫기 → 종료 확인 |
| 화면 | 가로·세로 | 세로 고정, 토스 버튼 자리 비움 |
| 폰트 | CDN | 번들에 포함 |
| 음악 | 스테레오 256k | 모노 128k |

토스와 닿는 코드는 전부 `src/platform/`에 있습니다. SDK는 `src/platform/toss.ts`
한 곳에서만 import합니다. SDK 함수는 토스 밖에서 호출하면 예외를 던지기 때문에,
이 파일이 모든 호출을 감싸서 일반 브라우저에서는 조용히 넘어가게 합니다.
그래서 같은 빌드가 브라우저에서도 그대로 돌아갑니다(등급분류 제출용).

## 빌드

```bash
npm ci
```

로컬에서 눈으로 확인할 때 (토스 버튼·노치를 흉내 낸 화면):

```bash
CI=false GENERATE_SOURCEMAP=false REACT_APP_TOSS_SIM=1 npx react-scripts build
```

빌드를 서버로 띄운 뒤 주소 끝에 `?tossSim=1`을 붙입니다. 콘솔에서 `__tossBack()`을
부르면 안드로이드 뒤로가기를 누른 것과 같습니다. 이 스위치는 출시 빌드에는 없습니다.

**토스에 올릴 번들은 로컬에서 만들지 않습니다.** Windows의 `ait build`는 번들 안
경로를 역슬래시로 적습니다. GitHub Actions(리눅스)가 만듭니다.

- `toss` 브랜치에 push → 번들을 만들어 Actions 결과물로 남김
- `toss-`로 시작하는 태그를 push → 번들을 만들어 **토스 콘솔에 업로드** (QR 테스트 가능)

```bash
git tag toss-0.1.0-1
```

```bash
git push origin toss-0.1.0-1
```

콘솔 업로드에는 GitHub 저장소 Secret `AIT_API_KEY`가 필요합니다
(콘솔 → 워크스페이스 → 왼쪽 메뉴 "키"에서 발급).

업로드는 출시가 아닙니다. 콘솔에서 QR 테스트 → 검토 요청 → 승인 → "출시하기"를
직접 눌러야 사용자에게 나갑니다.

## 토스판에만 있는 것

원작(`main`)의 규칙은 그대로 두고, 그 바깥에 얹은 것들입니다. 기본값에서는 한 판의 진행이
원작과 프레임 단위로 같습니다(`qa/parity`).

| 기능 | 어디 | 메모 |
|---|---|---|
| 오늘의 도시 | `src/platform/daily.ts` | 한국 날짜로 하루에 시드 하나(`daily-YYYY-MM-DD`). 미로·생선·집 위치는 모두에게 같고, 아파트는 고양이 위치를 따라 서므로 사람마다 다릅니다. 점수는 같은 토스 랭킹에 올라가고, 오늘 최고·연속 일수는 기기에 남습니다. |
| 엔딩 도감 | `src/components/EndingsPanel.tsx`, `records.ts` | 끝 8가지(집 도착 1 + 게임 오버 7). 본 것만 제목이 보입니다. |
| 화면 효과 줄이기 · 진동 | `src/game/core/comfort.ts`, 설정 | 흔들림 1/4, 글리치 찢김 없음, 붉은 화면은 깜빡이지 않고 고정. 그리는 것만 바꾸고 판정은 건드리지 않습니다. 폰의 "동작 줄이기"가 켜져 있으면 자동으로 적용됩니다. |
| 빙판 | `src/game/core/mazeGrid.ts`(생성), `src/game/systems/IceField.ts`(그리기), `Player.ts`(미끄러짐) | 아래 |
| 광고 | `src/platform/ads.ts` | 아래 |

### 빙판

빙판 칸에 올라서면 들어간 방향으로 계속 미끄러집니다. 빙판이 끝나는 칸(일반 도로)에서 멈추고,
벽·아파트에 닿아도 멈춥니다. 미끄러지는 동안 조작은 무시되고, **점프는 예외**라서 우유를 쓰면
빠져나올 수 있습니다. 빙판 위에 가만히 서 있을 수는 없습니다(갈 수 있는 방향이 있으면 끌려감).

- 한 칸짜리가 아니라 **3–9칸 구간**으로 깔립니다. 시작 지점과 집 주변 2칸은 얼지 않습니다.
- 적(검은 고양이)은 미끄러지지 않습니다. 빙판은 플레이어의 위험이자 지름길입니다.
- 지금은 **오늘의 도시에만** 나옵니다. 일반 플레이는 빙판이 0이고, 생성기가 난수를 한 번도
  더 뽑지 않아 원작과 프레임 단위로 같습니다.
- 종류별 비율은 `src/platform/cityPlans.ts`. 테스트 빌드에서 `?plan=glacier`로 강제할 수 있습니다
  (출시 빌드는 무시).

20종 계획: `store-assets/plan/오늘의-도시-20종.md`

### 광고

두 자리뿐이고, 둘 다 판과 판 사이입니다. 플레이 중에는 아무것도 뜨지 않습니다.

- **전면 광고**: 결과 화면에서 "다시"나 "메뉴로"를 눌러 나갈 때. 방문 후 첫 판 뒤에는 안 뜨고,
  마지막 광고로부터 150초가 지나야 다시 뜹니다(`AD_POLICY`). 미리 받아 둔 광고가 없으면 기다리지 않고 그냥 넘어갑니다.
- **보상형 광고**: 결과 화면의 "광고 보고 우유 한 팩 들고 시작" 버튼. 끝까지 보면 다음 판을 점프 2개로 시작합니다
  (`userEarnedReward` 이벤트가 와야만 지급). 보상형을 봤으면 그 직후의 전면 광고는 건너뜁니다.
- 광고가 떠 있는 동안 게임 소리는 꺼지고, 닫히면 돌아옵니다.

광고 그룹 ID는 콘솔(워크스페이스 → 광고)에서 전면 1개, 보상형 1개를 만들어 받습니다. 사업자 등록과
정산 정보 승인이 먼저입니다. 받은 ID는 GitHub 저장소 Settings → Secrets and variables → Actions →
**Variables** 탭에 넣습니다(비밀값이 아닙니다).

| 이름 | 값 |
|---|---|
| `AD_INTERSTITIAL_ID` | 전면 광고 그룹 ID |
| `AD_REWARDED_ID` | 보상형 광고 그룹 ID. 콘솔의 보상 이름은 "우유", 수량 1 |

비워 두면 SDK의 테스트 광고 ID로 빌드됩니다. QR 테스트에는 그게 맞고(실제 ID로 테스트하면 정책 위반),
**검토에 낼 번들은 두 값을 넣은 뒤에 만든 것이어야 합니다**(체크리스트: "테스트 광고가 아닌 실제 광고가 정상 노출").

브라우저에서는 `?tossSim=1`일 때 가짜 광고 화면이 1.5초 떠서 흐름을 확인할 수 있습니다.

## 바꾸면 안 되는 것

- `apps-in-toss.config.ts`의 `appName: 'mazewhiskers'` — 콘솔 등록값과 같아야 하고,
  콘솔에서는 등록 후 수정·삭제가 안 됩니다.
- 게임 안의 이름 "메이즈 위스커스" — 콘솔 앱 이름, 등급분류 신청서의 게임물명과
  같아야 합니다.
